* prompt & task
Fix compile errors in describe_stack_drift.rs after moving logic into handlers.

* requirements
- Understand existing code in iidy/src/cfn/describe_stack_drift.rs and iidy-js reference.
- Ensure DescribeStackResourceDriftsPaginator uses correct method (maybe items or send
  with aggregator). Fix compile error.
- Ensure PropertyDifference implements Serialize or convert to other method to print YAML.
- Run `cargo build --target=x86_64-unknown-linux-gnu` after changes but kill if >1min.
- Commit plan before code.

* implementation-plan
- [ ] inspect iidy/src/cfn/describe_stack_drift.rs to see usage of DescribeStackResourceDriftsPaginator::items.
- [ ] check AW SDK for Rust for method name (maybe `items` or `send`). Use introspection with cargo doc or code search in repo's dependencies (target). But we may not have docs; maybe use `send()` from smithy clients. We'll check compile error message to determine.
- [ ] If `items` doesn't exist, use `send().await` with .into_paginator().items() or something? Wait compile error shows no method items; there might be `send()` or `collect()`? maybe `try_collect`. We'll open target doc or check other modules.
- [ ] fix property difference serialization: implement `Serialize` for this struct or convert to YAML using manual mapping.
- [ ] run cargo build with target and fix compile errors.
- [ ] commit code changes.
