* prompt & task
Prompt:
#+begin_quote
Implement describe-stack-drift. Architect it on a way that you can test offline with fixtures. Study the aws IO / presentation separation in list-stacks etc
#+end_quote

* requirements
- Separate AWS api IO from output formatting and terminal IO
- Ensure offline testing possible
- Ensure DescribeStackResourceDriftsPaginator uses correct method (maybe items or send
  with aggregator). Fix compile error.
- Ensure PropertyDifference implements Serialize or convert to other method to print YAML.

* implementation-plan
- [X] Implement a new format_resource_drifts function
- [X] Complete describe_stack_drift workflow that triggers drift detection, waits for completion, fetches resource drifts, and formats the output for display
- [X] Add unit tests covering drift formatting scenarios
- [X] Integrate the new command into main.rs so it executes the drift logic and prints formatted lines
