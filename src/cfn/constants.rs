// Default poll interval for watch operations (seconds)
pub const DEFAULT_POLL_INTERVAL_SECS: u64 = 2;

// Default timeout for polling operations (seconds) - 1 hour
pub const DEFAULT_POLL_TIMEOUT_SECS: u64 = 3600;

// Default number of previous events to show when displaying stack events
pub const DEFAULT_PREVIOUS_EVENTS_COUNT: usize = 10;

// Changeset creation timeout (seconds) - 5 minutes
pub const MAX_CHANGESET_CREATION_TIMEOUT_SECS: u64 = 300;

// Changeset creation polling interval (seconds)
pub const CHANGESET_POLL_INTERVAL_SECS: u64 = 2;
